class App
{
    runApplication(){
    
        console.log("hello world!");
        let randomGetal = Math.random()

        if (randomGetal == 0 );
            
        if (randomGetal > 0.2);
        
        if (randomGetal > 0.6);
        
    }
    
    
}

let app = new App();
app.runApplication();
